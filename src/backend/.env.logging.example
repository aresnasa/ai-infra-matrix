# 环境变量示例文件
# 将此文件复制为 .env.example 或直接修改 .env 文件

# 日志级别配置选项：
# - trace: 最详细的日志，包含所有调试信息
# - debug: 调试级别，用于开发和问题排查
# - info:  常规信息级别，生产环境推荐 (默认)
# - warn:  警告级别，只记录警告和错误
# - error: 错误级别，只记录错误信息
# - fatal: 致命错误级别
# - panic: 最高级别，记录后程序会panic

# 开发环境建议使用 debug 或 trace
LOG_LEVEL=debug

# 生产环境建议使用 info 或 warn
# LOG_LEVEL=info

# 测试环境可以使用较高级别
# LOG_LEVEL=error

# 示例：通过docker-compose启动时覆盖日志级别
# docker-compose up -d
# 或者
# LOG_LEVEL=debug docker-compose up -d

# 其他配置...
PORT=8082
DB_HOST=localhost
DB_PORT=5432
# ... 其他环境变量保持不变
